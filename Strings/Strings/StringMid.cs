using System;
using System.Collections.Generic;
using System.Text;

namespace Strings
{
    internal class StringMid
    {
       public static void MidValue()
        {
            String str = "sridhar";
            int mid = str.Length / 2;
            Console.WriteLine(str[mid] );
        }
    }
}
